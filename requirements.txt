# Matches the scmp_llm_llama environment so SC quality numbers are comparable.
torch==2.5.1+cu124
torchaudio==2.5.1+cu124
torchvision==0.20.1+cu124
transformers==4.51.3
tokenizers==0.21.1
huggingface-hub==0.30.2
safetensors==0.5.3
accelerate==1.6.0
einops==0.8.1
numpy==2.0.2
tqdm==4.67.1
triton==3.1.0
ninja==1.11.1.4
# scmp_kernels supplies sc_matmul — install editable from the sibling repo:
#   pip install -e ../scmp_kernels
